<h1><%= @owner.username %>'s plant fam</h1>
<ul>
<% @owner.plants.each do |plant|%>
<li>
<%= link_to plant.name, plant_type_path(plant.plant_type.id)%><%= button_to "Delete Plant", plant_path(plant.id), method: :delete%>
</li>
<li>
<%= link_to plant.name, plant_type_path(plant.plant_type.id)%><%= button_to "Edit Plant Name", plant_path(plant.id), method: :post%>
</li>
<%end%>
</ul>

<%# <h2>Got New Plant Babies? Add 'em Here!</h2>
<%= form_for @owner do |f| %>
<%# <label>Plant Name</label><br> %>
<%# <%= f.text_field :name %>
<%# <%= f.collection_select :plant_type_id, @plant_types, :id, :species%> 
<%# <%end%>

<%= button_to "Add New Plant", new_plant_path, method: :get %>
